export type CategoryKey = 'survival' | 'development' | 'protection' | 'participation';

export interface CategoryInfo {
  name: string;
  color: string;
  icon: string;
  bgLight: string;
  textDark: string;
}
